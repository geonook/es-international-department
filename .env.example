# ES International Department - Environment Variables Template
# ES 國際部環境變數範本 - 複製此檔案並重命名為相應環境配置
# Copy this file and rename to appropriate environment configuration

# ==========================================
# DATABASE CONFIGURATION | 資料庫配置
# ==========================================
# Zeabur PostgreSQL Database URLs for different environments
# 從 Zeabur 控制台複製對應環境的資料庫連接字串
# Copy the database connection string from Zeabur console for corresponding environment

# Development Environment (開發環境) - Port 32718
# DATABASE_URL="postgresql://username:password@tpe1.clusters.zeabur.com:32718/zeabur"

# Staging Environment (預備環境) - Port 30592
# DATABASE_URL="postgresql://username:password@tpe1.clusters.zeabur.com:30592/zeabur"

# Production Environment (正式環境) - Port 32312
# DATABASE_URL="postgresql://username:password@tpe1.clusters.zeabur.com:32312/zeabur"

DATABASE_URL="your_zeabur_database_connection_string_here"

# ==========================================
# APPLICATION CONFIGURATION | 應用程式配置
# ==========================================

# Application Environment
NODE_ENV="development"  # development | staging | production

# Next.js Configuration
NEXTAUTH_SECRET="your-nextauth-secret-key-here"
# Development: http://localhost:3001 (注意：開發環境使用 3001 端口)
# Staging: https://staging.your-domain.com
# Production: https://your-domain.com
NEXTAUTH_URL="http://localhost:3001"

# JWT Secret for Authentication
JWT_SECRET="your-super-secret-jwt-key-here"

# ==========================================
# AUTHENTICATION SERVICES | 認證服務配置
# ==========================================

# Google OAuth Configuration (Google OAuth 配置)
# Obtain from: https://console.developers.google.com/
# 從 Google 開發者控制台獲取：https://console.developers.google.com/
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# OAuth Redirect URIs (OAuth 重定向 URI)
# Development: http://localhost:3001/api/auth/callback/google
# Staging: https://staging.your-domain.com/api/auth/callback/google  
# Production: https://your-domain.com/api/auth/callback/google
# Make sure to add these to your Google OAuth app settings
# 確保將這些 URI 添加到 Google OAuth 應用設定中

# ==========================================
# EMAIL SERVICE CONFIGURATION | 電子郵件服務配置
# ==========================================

# Email Service Provider (gmail, sendgrid, aws-ses, smtp)
# 電子郵件服務供應商 (gmail, sendgrid, aws-ses, smtp)
EMAIL_PROVIDER="smtp"

# SMTP Configuration (通用 SMTP 配置)
EMAIL_HOST="smtp.gmail.com"
EMAIL_PORT="587"
EMAIL_SECURE="false"  # true for 465, false for other ports
EMAIL_USER="your-email@gmail.com"
EMAIL_PASS="your-app-password"  # Use app password for Gmail

# Email Settings (郵件設定)
EMAIL_FROM="noreply@kcislk.ntpc.edu.tw"
EMAIL_FROM_NAME="KCISLK ESID Info Hub"

# SendGrid Configuration (SendGrid 配置 - 可選)
# SENDGRID_API_KEY="your-sendgrid-api-key"
# SENDGRID_FROM_EMAIL="noreply@kcislk.ntpc.edu.tw"

# AWS SES Configuration (AWS SES 配置 - 可選)
# AWS_SES_REGION="us-east-1"
# AWS_SES_ACCESS_KEY_ID="your-access-key-id"
# AWS_SES_SECRET_ACCESS_KEY="your-secret-access-key"
# AWS_SES_FROM_EMAIL="noreply@kcislk.ntpc.edu.tw"

# Email Queue Configuration (郵件佇列配置)
EMAIL_QUEUE_ENABLED="true"
EMAIL_QUEUE_BATCH_SIZE="10"  # Number of emails to process per batch
EMAIL_QUEUE_DELAY="1000"     # Delay between batches in milliseconds
EMAIL_RETRY_ATTEMPTS="3"     # Number of retry attempts for failed emails

# Email Rate Limiting (郵件速率限制)
EMAIL_RATE_LIMIT_PER_MINUTE="60"  # Max emails per minute
EMAIL_RATE_LIMIT_PER_HOUR="1000"  # Max emails per hour

# Email Testing (郵件測試)
EMAIL_TEST_MODE="false"      # Enable test mode (emails won't be sent)
EMAIL_TEST_RECIPIENT=""      # Test recipient for all emails in test mode

# ==========================================
# THIRD-PARTY SERVICES | 第三方服務配置
# ==========================================

# Cache Configuration (Optional - 快取配置，可選)
# REDIS_URL="redis://username:password@redis.zeabur.com:6379"

# File Storage Configuration (檔案存儲配置)
# Choose one: Vercel Blob OR AWS S3
# 選擇一種：Vercel Blob 或 AWS S3

# Vercel Blob (Recommended - 推薦)
# VERCEL_BLOB_READ_WRITE_TOKEN="your-blob-token-here"

# AWS S3 (Alternative - 替代方案)
# AWS_ACCESS_KEY_ID="your-access-key-here"
# AWS_SECRET_ACCESS_KEY="your-secret-key-here"
# AWS_REGION="ap-northeast-1"
# AWS_S3_BUCKET="es-international-files"

# ==========================================
# MONITORING & LOGGING | 監控與日誌配置
# ==========================================

# Error Tracking (Optional - 錯誤追蹤，可選)
# SENTRY_DSN="your-sentry-dsn-here"

# Analytics (Optional - 分析服務，可選)
# GOOGLE_ANALYTICS_ID="your-ga-id-here"

# ==========================================
# SECURITY CONFIGURATION | 安全配置
# ==========================================

# CORS Origins (以逗號分隔 - comma separated)
# Development: http://localhost:3001,http://127.0.0.1:3001
# Staging: https://staging.your-domain.com
# Production: https://your-domain.com
ALLOWED_ORIGINS="http://localhost:3001"

# Rate Limiting (速率限制)
RATE_LIMIT_MAX_REQUESTS="100"
RATE_LIMIT_WINDOW_MS="900000"  # 15 minutes

# ==========================================
# ZEABUR DEPLOYMENT SETTINGS | Zeabur 部署設定
# ==========================================

# Build Configuration
SKIP_ENV_VALIDATION="1"  # Skip env validation during build

# Deployment Region (部署區域)
# ZEABUR_REGION="ap-east"  # Available: us-west, eu-central, ap-east

# ==========================================
# DEVELOPMENT TOOLS | 開發工具配置
# ==========================================

# Prisma Configuration
PRISMA_CLI_TELEMETRY_DISABLED="1"

# Debug Mode (開發時啟用 - enable during development)
# DEBUG="prisma:*"
# PRISMA_LOG_LEVEL="info"

# Environment validation (建置時跳過)
SKIP_ENV_VALIDATION="1"

# ==========================================
# TINYMCE RICH TEXT EDITOR | TinyMCE 富文本編輯器
# ==========================================
# TinyMCE API Key (可選 - using GPL license)
# TINYMCE_API_KEY="your-tinymce-api-key"
# NEXT_PUBLIC_TINYMCE_API_KEY="your-tinymce-api-key"

# ==========================================
# NOTES | 使用說明
# ==========================================

# 1. 複製此檔案為 .env.development, .env.staging, 或 .env.production
#    Copy this file as .env.development, .env.staging, or .env.production

# 2. 從 Zeabur 控制台獲取對應環境的資料庫連接字串
#    Get the database connection string from Zeabur console for the corresponding environment

# 3. 絕對不要將包含機密資訊的 .env 檔案提交到版本控制系統
#    NEVER commit .env files containing sensitive information to version control

# 4. 在 Zeabur 部署時，環境變數會自動從控制台注入
#    During Zeabur deployment, environment variables are automatically injected from the console

# ==========================================
# ENVIRONMENT SPECIFIC PORTS | 環境專用端口
# ==========================================
# Development: localhost:3001 (npm run dev)
# Production: 8080 (npm run start - Docker 內部端口)
# 開發環境：localhost:3001 (npm run dev)
# 生產環境：8080 (npm run start - Docker 內部端口)